# Фауна: прогресс реализации

Исходное ТЗ v2 прочитано целиком: `D:\Downloads\drevlepuscha_wildlife_codex_spec.md` (18.09.2026).
Работа локально в `codex/wildlife-w0-w2`; публикация отдельно не согласована, push не выполнять.

## W0 — проверено

- Baseline: `bcb9d1a8e39c95457557ae6264a107eb4e2f58ee`, исходная ветка `main`.
- Прочитаны текущие AGENTS.md, README, DEVELOPMENT и точки генерации/кадра/комнат/карты из ТЗ.
- С исследовательского `d670e02` изменились дождь, звуки дождя, ambient и соответствующие проверки; эти изменения сохраняются.
- Чужие исходные untracked: `tools/3d/`, `tools/audio/`; не включать в коммиты.
- Node 22.23.1, node_modules доступны. Blender найден в `C:\Program Files\Blender Foundation\Blender 5.2`; анимационный экспорт ещё не проверен.
- Исходный `npm run test:showcase`: PASS, 1 сценарий, установленный Chrome/WebGPU, Windows, 1280×720. Mac/iPhone не проверялись.
- Golden снят **до** изменения генератора: `tests/fixtures/wildlife-forest-golden.json`. SHA256 всех полей/порядка для четырёх вариантов + исходная финальная посадка дополнительных семейств.

## Инструменты и расходы

- Пользователь разрешил установить официальный Meshy MCP и предоставил общий лимит **400 кредитов на весь живой лес**, включая remesh/retexture.
- Установлен `@meshy-ai/meshy-mcp-server@0.5.1` в пользовательский `.codex/tools/meshy`, отдельно от игры. Подтверждён stdio initialize/tools/list и успешный read-only check_balance.
- Ключ хранится вне репозитория, Windows DPAPI для текущего пользователя; в Codex зарегистрирован launcher без ключа в аргументах/конфигурации.
- Схемы tools/list сохранены локально в `.codex/tools/meshy/capabilities.json`. В игровом коде, сервере и CI Meshy отсутствует.
- Платные задачи этой работы: **0**, списаний от созданных нами задач: **0**. Task IDs пока нет. Остаток согласованного бюджета: 400.
- Выбор для будущего первого вида: single-image, Meshy 7 standard, triangle, без Ultra/8K/humanoid pose. Оценка перед запуском обязательна; качество четырёхугольников ранее не устраивало пользователя. Rig/animation — Blender.

## Текущий этап

W1: программная часть реализована; адресные Node-тесты и typecheck прошли.

- Общие явные mode/heightAt для размещения и выбора семейства. Golden совпал во всех четырёх случаях и после финальной посадки.
- Неизменяемый каталог с абсолютными матрицами, ID, family/version и bounds; доступен runtime и компилятору.
- Компилятор использует все 4372 дерева и 18 props с реальными BVH/матрицами. Одна точка птицы и два криволинейных пути проверены цилиндром с шагом ≤4 см после сглаживания. Насест ниже неподвижного порога WGSL ветра (18% высоты модели). Укрытия проверяются на перекрытие взгляда с тропы; удаление при близком наблюдателе откладывается.
- Тестовый насест: `showcase--16--104`, family `meshy-fork-oak-v1-2`. Координаты получены из треугольника модели, сохранены в `config/wildlife/showcase-sites.json`; это ещё не художественная приёмка места.
- WildlifeWorld: птица, общие метрические наблюдатели, фиксированные 200 мс, ограниченный reconcile, аналитическое движение, безопасный выбор пути, отдельный RNG, квоты и pinned cells. Snapshot не меняет мир.
- 7 адресных тестов PASS: golden, компиляция/валидация, inverse-transpose, 30/60/120 Гц, read-only snapshot, сон, второй наблюдатель/закрытые выходы, origin и выгрузка.
- Content hash в `public/wildlife/content/manifest.json`; модели отсутствуют. Production-валидация намеренно отвергает test-only manifest.

## W2 — программные критерии пройдены (19.09.2026)

- `WildlifeSession` разделяет владельца и реплику; передача solo → host сохраняет сам экземпляр ядра. Выход из комнаты восстанавливает видимые состояния в новой solo-эпохе, без повторения событий; это не сохранение полной индивидуальной истории.
- Capability `wildlife-v1` проверяет realm/contentHash/behaviorVersion. Кадры ограничены 32 KiB UTF-8, ID/state/site/route проверяются до применения, старые seq/epoch отвергаются. Новая эпоха принимается только в приветствии известного владельца. Несовместимость показывается текстом.
- Решения выполняются один раз в существующем тике комнаты; гостевые кадры хранятся до загрузки 3D. Есть общие часы браузерного хоста и ограниченная поправка RTT. Меню/атлас не останавливают общую симуляцию. Loading-клиенты исключены из наблюдателей, в том числе после замены страницы постоянной комнаты.
- Один и тот же тестовый эпизод работает solo, host/guest и в Node. Node использует подготовленный JSON без GPU/DOM, после restart создаёт новую authorityEpoch и сохраняет прежнее приглашение/идентичность сервера. Пустая комната не тикает.
- Общий renderer принимает абсолютные ENH и origin, не знает границ шоукейса. Использует общий FrameWorkBudget, реальные списки теней/quality/dispose. Один ярко-розовый примитив и надпись «ТЕСТОВАЯ МОДЕЛЬ» доступны только с `debug=1&wildlife=1`; это не художественная птица. Голубые линии маршрутов включаются отдельно через `m0.wildlife.setProbe(true)`.
- В живой сцене полностью совпали каталог 4372 деревьев и порядок obstacle ID, отдельно bounds 18 props. Для обзора птицы используются приближённые стволы/props, для проверки полётного коридора — полная исходная геометрия.

### Проверено

- `npm run test:wildlife` — **15/15 PASS**: golden, воспроизводимый компилятор, координаты/нормали, чистота ядра, FPS-независимость, закрытые выходы, события/эпохи, шесть наблюдателей и квоты, перенос core и renderer/rebase. Renderer-тест с NullEngine проверяет данные и lifecycle, не качество WebGPU.
- `npm run test:server` — **13/13 PASS**, существующие проверки протокола/хранилища постоянной комнаты.
- Три адресных WebGPU-сценария — **PASS** в Chrome/Windows, 1280×720: `showcase-wildlife.spec.mjs`, `showcase-wildlife-multiplayer.spec.mjs`, `persistent-wildlife.spec.mjs`.
- Две настоящие сцены: один ID/route, общий взлёт от гостя за 120 м от ведущего, меню/атлас, поздний вход; удержание модели гостя >10 секунд и сохранение того же RTC. Дополнительно четыре лёгких клиента заполняют комнату до шести, седьмой получает `full`. Это проверка вместимости, не замер шести игровых устройств.
- Постоянная комната: общая птица, загрузка после reload исключает наблюдателя, restart меняет эпоху без призраков, повторный вход по прежней ссылке, прекращение тика после выхода всех.
- `npm run build` — **PASS**, включая typecheck и wildlife:validate. World pack unchanged. Vite сообщает о крупных общих chunks; FPS и загрузку на целевых устройствах не измеряли.
- Diff проверен: камера, география и `public/connection-baseline` не изменены; чужие `tools/3d/` и `tools/audio/` не включены.

### Возобновление

- Коммиты до W2: W0 `ee0907a`, W1 `4d29132`; W2 сохраняется следующим отдельным коммитом в `codex/wildlife-w0-w2`.
- Запуск: `npm run dev`, затем `http://127.0.0.1:5173/?debug=1&wildlife=1`. Приглашение из этой сцены сохраняет тестовые флаги. Обычный `/` не включает фауну.
- Локальная постоянная комната: `npm run server -- --wildlife test --client-url http://127.0.0.1:5173/ --data-dir .server-data/wildlife`. Её ссылку с секретом не сохранять в Git.
- Подготовленный contentHash: `2b16265cfaade133c572c51144083f8ec735ee702d542d3b46263f989fb1fcde`. Хэши текстовых исходников нормализуют CRLF в LF для совпадения Windows/Linux. При изменении исходных данных: `npm run wildlife:build`, затем адресная валидация. `--production` отвергает отсутствие принятых художественных ассетов.
- Возможности и выбор Meshy: [WILDLIFE-MESHY.md](WILDLIFE-MESHY.md). Приватный журнал: `.artwork/wildlife/jobs.json`, общий лимит 400, задач/расходов пока 0. Не повторять неизвестно завершившийся платный запрос.
- Следующий этап **W3**: один референс и один кандидат птицы через официальный Meshy MCP, проверка геометрии, затем Blender rig/clips/GLB и звук. Сначала закончить один вид, затем W4/W5 и размножение мест. Ключ и бюджет уже предоставлены; дополнительное согласование лимита не требуется.
- W3–W8 ещё не выполнены. Реальная проба адаптера на карте 1:1 относится к W7; сейчас пройдены только переносимые fixture/core/renderer проверки. Mac M3/iPhone, художественная приёмка, анимации, животные звуки и performance не проверены. Публикации и push нет; режим публикации не согласован.

## 2026-09-19 · W3: первая птица-кандидат в локальном шоукейсе

**Статус:** технический кандидат, W3 ещё не принят. W0–W2 завершены коммитом `b80ef1b`. Публикации/push нет. Следующий шаг — закончить визуальную и звуковую приёмку этого эпизода с обычной камеры; до неё не размножать места и не переходить к массовой генерации.

- Пользователь разрешил самостоятельно создавать референсы, ориентируясь на текущий лес и иллюстрации Толкиена. Выбраны приглушённые серо-коричневые, кремовые и ржаво-охристые пятна, цельный силуэт без мелких перьев.
- Через официальный Meshy MCP 0.5.1 выполнены ровно две оплаченные задачи: один референс nano-banana-pro (9 кредитов), затем image-to-3d Meshy 7 standard (30). **Расход 39 / 400, остаток 361, резерв 0.** Новых remesh/retexture не запускали. Task IDs, ответы и signed URLs — только `.artwork/wildlife/jobs.json` и `.artwork/wildlife/bird-01/`.
- Исходник Meshy: 3608 треугольников, нейтральная поза с открытыми крыльями; выбрана треугольная topology, без quad-remesh. Текстура и исходная геометрия только Meshy. Blender 5.2.1: сварка разрывов UV для весов, масштаб, редукция, 14 костей, клипы `perch_idle` 4 с, `alert` 0.6 с, `takeoff` 0.4 с, `fly_loop` 0.24 с; root motion отсутствует. Сложенные крылья исправлялись локально, без повторной оплаты.
- `public/wildlife/woodland-bird/lod{0,1,2}.glb`: **1200 / 480 / 150** треугольников; **537504 / 476864 / 440968** байт. Каждый файл: один материал, один PNG-атлас 512², один drawable primitive, максимум 4 skin influences. Одинаковый риг и клипы на LOD. Counts и SHA-256 проверяются из GLB, а не из обещаний генератора.
- Рецепт: `tools/wildlife/blender/export.py`, `config/wildlife/bird-rig-guide.json`. Команда: `blender --background --factory-startup --python-exit-code 1 --python tools/wildlife/blender/export.py -- .artwork/wildlife/bird-01/source.glb`; затем `node tools/wildlife/asset-manifest.mjs` и `npm run wildlife:build`. Blender MCP недоступен, использован отдельный background Blender CLI без изменения пользовательской сцены.
- Экспорт измеряет деформированные треугольники каждого кадра всех клипов/LOD; `bird-envelope.json` хранит вертикальные сечения. Компилятор проверяет сложенную птицу на опоре, раскрытие при взлёте и весь размах при полёте по исходной геометрии деревьев/props. Старые маршруты W2 не прошли проверку: исправлены только wildlife controls и безопасные воздушные точки укрытий относительно прежних tree anchors. География и деревья не менялись.
- Общий renderer клонирует GLB/skeleton, вручную сэмплирует фазу по времени authority; фоновых Babylon animatables нет. Загрузка максимум двух шаблонов одновременно, создание экземпляров через существующий FrameWorkBudget; независимые экземпляры и освобождение при уходе/dispose. Профили: 4/6/8 ближних скелетов, LOD-переходы с гистерезисом, дальние позы 12/6 Гц. Максимум общих сущностей не зависит от качества. Это ограничения работы, не замер FPS.
- Событие bird-flush подаётся в существующий ShowcaseAudio/B04. Новый AudioContext не создаётся; общий лимит двух голосов, TTL 500 мс после загрузки, пауза/mute/hidden не воспроизводят историю. При wildlife-режиме независимый случайный B04 отключён. Слуховая приёмка синхронизации в этом проходе не выполнена.
- Локальный вход: `http://127.0.0.1:5173/?debug=1&wildlife=1`. Нажать «Начать прогулку», затем «К птице»; «Подойти» провоцирует взлёт. «Отойти» оставляет естественный cooldown 90 секунд; для немедленного повторного solo-прохода перезагрузить страницу. Кнопки перемещают только героя в проверенную свободную точку с учётом текущего yaw; камеры не меняют. При сильно изменённом ракурсе точка может оказаться занята — кнопка сообщит это в подсказке.
- Есть указатель птицы и явная подпись «КАНДИДАТ». Для тестового шара сохранён отдельный `&wildlifeTestModel=1`. При ошибке загрузки модель остаётся явно тестовой. Обычный `/` по-прежнему не включает этот эксперимент.

### Проверено и ограничения

- TypeScript и `npm run build` — PASS, world pack unchanged. Остаётся существующее предупреждение о крупных Vite chunks.
- Все прежние 15 wildlife-тестов прошли после подключения renderer; добавлены 2 проверки реальных GLB/manifest и фазы/паузы анимаций. После последней правки envelope повторены 5 относящихся к ней asset/content тестов — PASS. Golden сохранения исходного леса проходит.
- В локальной WebGPU-сцене проверены загрузка LOD2→LOD0, `perch_idle`→`alert`→`fly_loop`, реакция на подход, отсутствие ошибок в консоли. Ближняя модель видна под указателем; размер и контраст ещё требуют художественной оценки. Проверки клипов в Blender и числовые envelope-проверки не заменяют её.
- W3 не закрыт: нужны оценка полного взлёта/силуэта/опоры с обычной камеры, слуховая проверка и отдельная визуальная проверка двух клиентов с новым GLB. Сетевые core/DTO не менялись, но W2-проверку на двух клиентах нельзя выдавать за W3 визуальную приёмку.
- ContentHash: `6e13b6d3a2f9ffe52fb19747ee6a3ba12d4566644f0e902304b5be6325ff61ba`. При смене пакета все участники должны открыть одинаковую новую сборку. `wildlife:validate -- --production` отвергает candidate/test-only; принятие ассета и разрешение публикации остаются разными действиями.
- W4–W8, Mac M3/iPhone, performance и реальный адаптер карты 1:1 ещё впереди. `tools/3d/`, `tools/audio/`, камера, география и connection-baseline не изменялись этой задачей.

## 2026-09-19 · W3: проверка двух клиентов и перенос эпизода

- Пользователь подтвердил продолжение всего исходного плана W0–W8 и предпочёл прямые Playwright-проверки управлению мышью. Продолжать реализацию этапами, не останавливаться после каждого отчёта. Публикация отдельно не разрешена.
- `showcase-relocated-site.json`: тот же модельный anchor на другом существующем экземпляре `meshy-fork-oak-v1-2`, дерево `showcase-16-312`, ENH `(16.2928, 314.7382, 17.3430)`. От первого насеста около 420 м; две новые кривые независимо проверены по деформированной оболочке птицы. Генерации нового ассета для переноса нет.
- Компилятор принимает набор мест, группирует их по ячейкам, включает фактические конфигурации в contentHash и экспортирует версионные `treeBindings`. Runtime больше не импортирует authoring JSON первого места. Новое место меняет данные, а не поведение/renderer.
- Debug-селектор «Место птицы»: выбрать второе место, «Отойти» для активации на расстоянии, затем «К птице»/«Подойти». Это две проверочные площадки, не массовое заселение леса.
- Playwright `showcase-wildlife-art.spec.mjs` — PASS: два настоящих WebGPU-клиента, LOD0 и реальный GLB, одинаковые ID/route, `alert`→`fly_loop`, B04 у каждого ровно один раз с задержкой ≤505 мс, нет автоматических animatables/ошибок, переход на второй реальный насест. Без управления мышью и без фотосессии.
- `test:wildlife` — 18/18 PASS, typecheck PASS. Исправлены два старых предположения тестов о единственной ячейке/двух debug-маршрутах. Отдельно проверены разные хэши разных конфигураций, неизменность локального anchor и отказ несовместимой привязки.
- Новый contentHash `53b43208b9ad8fe8490ad1e8c5e1c8bb4192b552ceeb4b22639fc42c0a2bd201`. Художественная/слуховая оценка человеком остаётся пометкой candidate и не заменяется автотестом; разработка W4 продолжается по явному указанию пользователя.
- W4: референс белки готов, задача Meshy 7 standard/triangle выполняется. Фактический расход 48/400, резерв на уже запущенную геометрию 30; сверять приватный журнал, не запускать дубликат. Один платный job одновременно.

## 2026-09-19 · W4–W5: общие эпизоды белки и косули, технические кандидаты

- Пользователь предпочитает прямое управление через API/CLI/Playwright. В этом проходе UI мышью/CUA не управляли. Продолжать весь W0–W8, не считать этот checkpoint завершением задачи. Push/публикация по-прежнему не разрешены отдельным режимом.
- Все три Meshy-исходника получены официальным MCP, standard Meshy 7 / triangle. Белка и косуля: по одному референсу (9) и одной модели (30). **Итого 117/400, резерв 0, остаток 283.** Повторных платных задач не было. Приватные квитанции/источники/Blender-файлы в `.artwork/wildlife`; не включать в Git.
- Blender CLI 5.2.1: `land_rig.py`, `land_animate.py`, `land_export.py`; анатомические landmarks и ограничения в двух `*-rig-guide.json`. Белка: 25 костей, 6 клипов, 2999/1199/299 треугольников, GLB 1747472/1621904/1539688 байт. Косуля: 27 костей, 4 клипа, 5000/2000/550 треугольников, 1633360/1494664/1409884 байт. По одному Meshy-атласу 1024², максимум 4 skin influences, без root motion. Все ассеты остаются candidate.
- Общий загрузчик теперь обслуживает все виды с суммарным пределом двух асинхронных загрузок. Renderer использует ENH, нормали опоры, общий бюджет создания экземпляров, независимые скелеты и ручное сэмплирование фаз. LOD не меняет размер/ID животного. Наземный путь содержит общую таблицу distance/time/state; переходы и паузы не создают скачка позиции.
- Белка: `squirrel-site.json`, реальное дерево `showcase-24-96`, family `meshy-young-tree-v1-2`. Проходит по земле, прыгает на нижнюю неподвижную часть ствола, поднимается на обратной от тропы стороне и задерживается там. Подбор идёт по исходным треугольникам коры. Ранний спиральный маршрут отклонён: местами терял контакт на 25 см. Текущий прямой подъём сохраняет опору; исчезновение разрешается только при удалении всех наблюдателей и перекрытой видимости, а не на глазах у близкого игрока.
- `contact_tracks.py` измеряет реальные деформированные вершины лап; `check-squirrel-contact.mjs` проверяет их по BVH коры в опорных фазах подъёма. Для текущего дерева по 164 измерения: LOD0 максимум **2.493 см**, LOD1 **2.549 см**, LOD2 **3.450 см**, попадания в опору есть во всех измерениях, проникновения не обнаружены. LOD2 превышает ориентир 3 см; близкий LOD0 проходит числовой ориентир. Это измерение лап, не полная визуальная приёмка или доказательство отсутствия всех пересечений тела.
- Косуля: `deer-site.json`, один край поляны, три проверенных выхода с направлениями примерно −48°, +151°, −143°. Offline-поиск обходит существующие стволы; каждый сегмент повторно проверяется цилиндром радиуса 0.95 м / высоты 1.35 м, уклон ≤25°. Для каждого выхода есть walk/flee timing. Выбор учитывает всех игроков; перекрытие впереди останавливает животное, освобождение продолжает прежний путь без телепорта. Повторный близкий подход повышает walk до flee. География/деревья не редактировались.
- Debug-селектор теперь «Место фауны», кнопка «К животному». Проверочные перемещения меняют только героя; ищут свободную точку с обзором, сохраняя yaw камеры. Указатель подписывает ближайший вид. Обычный `/` не включает wildlife; тестовые примитивы доступны отдельно через `wildlifeTestModel=1`.

### Проверено на Windows / Chrome / WebGPU

- `typecheck` PASS; `test:wildlife` 22/22 PASS после общих изменений (последняя проверка уточнения валидации записывается в коммите).
- Новый `wildlife-land.test.mjs`: непрерывность ground/mount/climb, единичные нормали, корректный network frame, три выхода косули, блокировка другим игроком, остановка/возобновление без скачка. Старые проверки птицы/сети/переносимости проходят.
- `showcase-wildlife-art.spec.mjs`: два WebGPU-клиента, реальная птица и свежий B04, PASS (15.4 с). `showcase-wildlife-land.spec.mjs`: белка ground→climb→idle (10.9 с), косуля GLB→run_loop (5.3 с), PASS. Проверки напрямую Playwright, один размер окна, без фотосессии. Первоначальный сбой белки выявил закрытую деревом точку подхода; исправлен debug-подбор точки с обзором.
- Manifest проверяет фактические хэши/бюджеты/клипы всех 9 GLB; production-режим отвергает candidate. Полную сборку после этих изменений ещё не запускали.

### Следующее продолжение (ещё не выполнено)

1. W4: проверить переход mount и контакт тела/земли, второй совместимый масштаб/наклон, разобраться с LOD2 3.45 см; сохранить числовые свидетельства без объявления художественной приёмки. Текущий support transform закреплён в данных и при изменении требует повторного авторинга.
2. W5: доработать позу `graze_idle` (короткая шея исходника, текущая поза требует художественной оценки), проверить походку с обычной камеры и фазу наземных моделей на двух клиентах. Звуки наземных животных ещё не подключены; не подменять их птицей.
3. W6: несколько мест через проверенные шаблоны, местные насекомые, плавное расписание/паузы по общим часам и дождю, существующий микшер без дублей, 10–15 минут прогулки. Не создавать произвольную геометрию животных в обход Meshy.
4. W7: ограниченный реальный world probe, подготовленные данные, отмена загрузки/rebase/выгрузка без глобального заселения и правок географии.
5. W8: локальные измерения обычного/предельного сценария, bytes/queues/dispose, финальная сборка и проверка приватных данных. Mac M3/iPhone остаются непроверенными. Художественная/слуховая приёмка и публикация — отдельные незакрытые пункты.

Камера, география, `public/connection-baseline`, чужие `tools/3d/` и `tools/audio/` сохранены. Не включать эти чужие каталоги в следующий коммит.

## 2026-09-19 · W4–W6: контакт, местные бабочки, расписание и предметный звук

**Последнее изменение объёма пользователем:** карту 1:1 пока не трогать, отрабатывать только шоукейс. W7 отложен по прямому указанию. Начатые в этом проходе неприменённые файлы world probe удалены; `src/world`, география и карта не изменены. Переносимые core/renderer и ранее пройденные fixture/rebase проверки остаются. Реальную переносимость на world больше не объявлять завершённой.

- Белка: два существующих дерева семейства `meshy-young-tree-v1-2` с разными scale/tilt: `showcase-24-96` и `showcase-24--40`. Добавлены проверка плавного mount, второй путь и контактные свидетельства с SHA всех LOD, guide и исходных точек маршрута. Новый offset 3 мм, уточнены лапы в Blender. Зазор опорных лап на коре **1.993 / 2.049 / 2.950 см** для LOD0/1/2; максимальное проникновение лап на втором дереве 2.83 мм. На земле максимальный зазор 2.793 см. Пробные вершины всего LOD0: 435580 и 420560 измерений по ground/mount/climb, проникновение максимум 1.76 мм в землю и 4.84 мм в кору. Это числовая проверка с дискретизацией, не художественная приёмка всех треугольников.
- Косуля: `graze_idle` переработан в Blender — наклон корпуса/шеи и опущенная голова при удержании четырёх опорных лап через IK. Renderer сглаживает смену направления; клипы косули смешиваются, циклы покоя видов имеют индивидуальные фазы. Три проверенных выхода сохранены. Деформации и выразительность ещё требуют просмотра человеком.
- Местный фон: один Meshy-исходник бабочки, Blender-риг из 3 костей и `flutter` 0.16 с. GLB **180 треугольников, 144592 байта, 256²**, без теней и автоматических animatables. Три проверенные площадки у тропы (`insect-sites.json`), радиус проверочного объёма 1.4 м. Профили 4/8/12 экземпляров, общий с животными загрузчик ≤2 и тот же FrameWorkBudget. Ветер и время приходят из существующих систем; местный фон плавно уходит при дожде/темноте.
- Meshy: **156/400 потрачено, резерв 0, остаток 244**. Всего четыре reference nano-banana-pro по 9 и четыре Meshy 7 standard triangle image-to-3D по 30. Официальный MCP **0.5.1**. Без quad-remesh, без дополнительных платных дублей. Задачи завершены; signed URLs, исходники и результаты остаются в `.artwork/wildlife`, ключ — вне Git. Новые платные задачи сейчас не нужны.
- Расписание чистого ядра использует общие сутки/дождь: индивидуальный порог рассвета и дождя, косуля чаще на краях дня; cooldown детерминированно варьируется 0.85–1.5 от базового. Уже видимые животные завершают эпизод, смена погоды не удаляет всех разом. Близко к игроку новое значимое животное не появляется.
- Два локальных звуковых кандидата через существующий Stable Audio 3 Medium/ComfyUI без облака: `WLS01` (2.972 с) и `WLD01` (3.532 с), mono Opus 96 kbps. Сохранены исходные FLAC, seed/workflow/processing hashes и естественные окончания; у косули удалено только 0.462 с начальной тишины. Модели ComfyUI освобождены после завершения своей очереди. **Слуховая приёмка не выполнена**, статус `candidate-unheard`.
- Звук подаётся в единственный существующий ShowcaseAudio. B04 для птицы, отдельные события для белки/косули, TTL 500 мс, общий лимит двух голосов, временное подавление новых фоновых B01/B02 рядом с видимым источником. Статические случайные B04 в этом режиме выключены. Нет нового микшера/AudioContext.
- Все модели остаются **candidate**, обычный `/` их автоматически не включает. Вход: `/?debug=1&wildlife=1`, селектор «Место фауны», «Отойти» → «К животному» → «Подойти». Тестовые примитивы отдельно: `wildlifeTestModel=1`. Публикация/push не согласованы и не выполнялись.

### Проверки этой итерации

- TypeScript PASS; 25/25 wildlife-тестов PASS перед добавлением двух финальных проверок. Повторная итоговая проверка записывается ниже.
- Прямой Playwright, Windows/Chrome/WebGPU, 1280×720: белка на двух реальных опорах и все клипы mount/climb, косуля GLB/run, два клиента и оба наземных звука один раз ≤505 мс — **3/3 PASS (37 с)**. Бабочка: загрузка, лимит, остановка в паузе, затухание в сильном дожде и dispose — **PASS (13.2 с)**. Управления мышью/CUA и фотосессии не было.
- Manifest теперь проверяет все **10 GLB** и оба Opus: хэши, counts, размеры, клипы, каналы и длительности. Production-режим не принимает candidate/unheard.
- Сборка PASS до финального уточнения очистки diagnostics/очередей; крупный существующий bundle warning не связан с wildlife. Итоговый build выполняется отдельно ниже.

### Следующее действие

Закончить W8: актуальные регрессии комнаты/сервера, финальный build, проверка отсутствия приватных данных и сохранение измерений. Затем техническая версия шоукейса доступна для художественного/слухового просмотра. W7 отложен пользователем; Mac M3/iPhone и 10–15 минут человеческой прогулки остаются непроверенными. Не выдавать это за финальную художественную версию леса.

## 2026-09-19 · W8: измерения и итог программной части шоукейса

Baseline этого этапа: `6f63307` (`feat(wildlife): complete showcase contact, insects and event audio`), ветка `codex/wildlife-w0-w2`. W7 отложен пользователем; отсутствующий world probe не выдаётся за выполненную приёмку переносимости.

- В существующий 20-секундный performance report добавлены `wildlifeControllerMs` и контекст: число моделей/LOD/детальных скелетов, очередь/загрузки, местные насекомые, размер wildlife snapshot и authority counters. Данные не включают ключи или private URL.
- Фактическое устройство: Windows, AMD Ryzen 5 7500F, NVIDIA GeForce RTX 5060; Chrome WebGPU, 1280×720, balanced. Обычный эпизод: три активных модели, 20 секунд, **2859 кадров, средний кадр 6.999 мс, p95 7.8 мс, максимум 80.2 мс**; CPU submit p95 7.2 мс; фауна p95 **0.4 мс**. Максимальный отдельный кадр не скрывается; эти значения не гарантируют отсутствие всех рывков.
- Отдельный, явно синтетический stress-сценарий: 24 животных трёх видов рядом, шесть наблюдателей чистого ядра (не шесть GPU-клиентов), тот же renderer/GLB, 20 секунд после подготовки. **2442 кадра, средний 8.198 мс, p95 9.8 мс, max 14.5 мс**; CPU submit p95 9.3 мс; core+snapshot+renderer+prepare p95 **0.7 мс**. Максимум две загрузки и шесть детальных скелетов по balanced. Snapshot ≤**8666 байт**, ниже 32768. После dispose skeleton/animatable counts вернулись к состоянию до тестового renderer (1/198 — существующий ranger/лес; добавка wildlife нулевая). Ассеты не создают собственных autoplay animatables. Контроллер фауны после dispose возвращает нулевое время, очищены pending/diagnostic состояния.
- GPU timestamp samples оказались редкими и не используются для заключения о полной стоимости GPU. iPhone 13 и Mac M3 **не проверены**; desktop mobile-emulation не подставлялась вместо устройств.
- `wildlife-long-session.test.mjs`: **15 минут модельного времени** с шестью движущимися наблюдателями, сумерками и дождём. 14 уникальных событий всех трёх видов, 17 появлений поколений, максимум 5 активных животных, snapshot ≤2486 байт, 4501 decision steps без clock reconciliation. После dispose — 0 active/cells/dormant. Это проверка поведения и ограничений, **не 15 минут человеческой прогулки** и не слуховая приёмка.
- Итоговые Node wildlife-тесты: **27/27 PASS**. Они включают golden неизменности леса, все готовые маршруты/опоры, явную переносимость fixture/origin, сети/валидаторы, контакт двух беличьих деревьев и полётные объёмы бабочек.
- Финальные адресные Playwright: постоянная комната, одинаковые снимки, restart/new epoch и пустая комната **PASS (51.9 с)**; каталог, тестовая птица, качество, пауза/dispose **PASS (5.7 с)**; ранний/поздний guest, удалённая угроза, atlas, шесть участников и отказ седьмому **PASS (53.5 с)**; stress **PASS (52.2 с)**. В первом общем запуске сетевой сценарий завершился ошибкой обращения к ещё недоступному m0. Вероятная причина — параллельное изменение импортируемого JSON и перезагрузка Vite; исходный catch скрыл первичную ошибку. Catch теперь сохраняет её, отдельный повтор после стабилизации файлов прошёл. Прежние проверки количества «ровно одна птица» заменены выбором конкретного siteId после появления наземных мест.
- Сырые локальные результаты: `tmp/wildlife-performance.json`, `tmp/wildlife-long-session.json`, `tmp/w8-*.log`; не являются художественными доказательствами и не публикуются. Воспроизводимые проверки: `npm run test:wildlife`, `npx playwright test tests/active/showcase-wildlife-budget.spec.mjs`, соответствующие wildlife specs.
- Сканирование исходников/server/config/public wildlife: ключей Meshy и signed asset URLs не найдено. Финальная сборка и скан dist фиксируются следующим абзацем после завершения команды.

**Статус для продолжения:** программная часть W0–W6 в шоукейсе и локальные проверки W8 реализованы. Три вида животных и бабочка — Meshy/Blender кандидаты; два новых звука — unheard candidates. Художественная/слуховая приёмка, длительная прогулка человеком и замеры на Mac M3/iPhone остаются открыты. Следующий практический шаг — просмотр текущих эпизодов обычной третьеличной камерой и правки по замечаниям; новые генерации без конкретной необходимости не запускать. Полную художественную готовность W3–W6 не объявлять. W7 — только после возобновления пользователем. Push и публикация не выполнялись; разрешение публикации отдельно отсутствует. Камера, география, `public/connection-baseline` и чужие `tools/3d/`, `tools/audio/` сохранены.

Финальный `npm run build` — **PASS** (TypeScript, content freshness, 10 GLB/2 Opus, Vite); **World pack unchanged**. В 321 текстовых файлах dist ключей Meshy и подписанных URL не найдено. Итоговый showcase contentHash: `25d5198e31d48363faa0dfeabac4e83644572e5e965b4f33eb1a15814458c2ea`. `git diff --check` PASS.

## 2026-09-19 · Публикация для проверки с компьютера и телефона

Пользователь разрешил публикацию текущей версии для проверки на компьютере/телефоне. Это публикация кандидатов шоукейса, не художественная приёмка. W7 и карта по-прежнему отложены. Публичный вход проверки: `https://amchercashin.github.io/drevlepuscha/?debug=1&wildlife=1`; обычный `/` не включает wildlife автоматически.

Перед отправкой поправлена только панель проверки: перенос кнопок на узком экране и положение выше джойстика при сенсорном вводе, статус ограничен шириной экрана. Прямой Playwright 390×844/hasTouch: кнопки в пределах viewport, джойстик не перекрыт, выбор белки/переход к реальной модели — PASS (11.5 с). Это проверка раскладки на Windows Chrome, не замер и не приёмка физического телефона. Кандидатные статусы и запрет production-acceptance для непринятых ассетов сохранены. Публикация штатным main → CI → GitHub Pages; итог CI/URL проверяется после отправки.

Публикация подтверждена: commit `390280234c5b3cdaa29269a1ac286bbd9910c53d`, GitHub Actions [35468938980](https://github.com/amchercashin/drevlepuscha/actions/runs/35468938980), check/deploy **success**. С публичного Pages получен ожидаемый contentHash `25d5198e31d48363faa0dfeabac4e83644572e5e965b4f33eb1a15814458c2ea`; прямой Playwright по публичному HTTPS URL загрузил WebGPU, реальную птицу LOD0/alert и новую панель, без page errors и asset errors. Локальный лог: `tmp/wildlife-published-smoke.log`. Проверки на физическом телефоне пока нет. Удобная ссылка текущей проверки: `https://amchercashin.github.io/drevlepuscha/?debug=1&wildlife=1&v=3902802`. Авторизация пользователя касается опубликованного кандидата, статусы художественной/слуховой приёмки не изменены.
